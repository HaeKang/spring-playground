spring:
# spring boot 는 기본적인 설정 없으면 메모리버전으로 걍 돌려버림 밑에 이 설정들 노필요
#  datasource:
##   h2 메모리 version
#    url: jdbc:h2:mem:test
#    username: root
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create
#    properties:
#      hibernate:
##        show_sql: true
#        format_sql : true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type : trace
